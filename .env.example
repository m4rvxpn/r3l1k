# Gemini API Keys (comma-separated for rotation when quota exhausted)
GEMINI_API_KEYS=<key1>,<key2>,etc..

# Ghostwriter Configuration
GHOSTWRITER_URL=https://ghostwriter.local/
GHOSTWRITER_API_KEY=<ghostwriter api>

# Optional: Company Name (can be overridden via CLI)
COMPANY_NAME=XYZ Compny
